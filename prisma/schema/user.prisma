model user {
  id                     String      @id @default(uuid())
  username               String      @unique
  password               String?
  email                  String      @unique
  name                   String?
}
